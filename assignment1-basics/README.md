# CS336 — 作业1说明

摘要：本目录包含 CS336 作业1 的实现代码。主要实现模块位于 `realize/` 子目录，包含模型组件与训练/评估相关的实用工具。

项目状态
- 所有可测试模块已实现并通过对应单元测试。  
- 尚未完成：最终的大作业（训练一个 decoder 模型）的训练部分。  
- 已知问题：BPE 相关的测试在内存占用上存在高峰（这是预期行为，测试因此被标记为失败）。

目录结构（与本作业相关）
- `realize/`：核心实现（优化器、embedding、attention、transformer block 等）。
- `tests/`：单元测试与测试夹具。  
- `data/`：示例/训练数据（若需本地训练，自行下载）。
- `assignment1-basics/`：本 README 和作业说明。

运行测试
1. 创建并激活合适的 Python 环境（推荐使用 `wsl2中配置uv`）。
2. 安装依赖（如有 `pyproject.toml` 或 `requirements.txt`）：



3. 在仓库根目录运行 pytest：（pdf中有具体测试和对应命令）

```bash
pytest 
```

注意：测试套件包含一个已知会因内存问题失败的 BPE 测试（这属于预期，不影响其它通过的测试）。

如何验证/调试你自己的实现
- 若要测试或替换部分实现（例如 tokenizer、adapter 等），可编辑 `tests/adapters.py` 中对应的测试点，或在测试夹具下添加新的用例。  
- 若修改了核心模块（`realize/`），请运行相关单元测试以确保没有回归。

建议
- 为避免在提交时带入临时/本地文件，请在仓库根目录添加或更新 `.gitignore`，忽略 `__pycache__/`、虚拟环境目录、IDE 配置等。  
- 若 BPE 部分理解思想是最重要的，一般应用场景中已经不会说让你从零写代码训练BPE。这部分对硬件要求比较高。
联系方式
- 

----
